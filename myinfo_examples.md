# myinfo命令使用示例

## 基本使用方法

### 命令格式
```
/myinfo
!myinfo
.myinfo
，myinfo
。myinfo
```

支持多种命令前缀，用户可以使用任意一种格式。

## 显示效果示例

### 1. 普通用户信息显示

#### 有Emby账户的用户
```
**· 🍉 TG&名称** | [张三](tg://user?id=123456789)
**· 🍒 识别のID** | `123456789`
**· 🍓 当前状态** | **正常**
**· 🍥 持有樱花币** | 150
**· 💠 账号名称** | zhangsan_emby
**· 🚨 到期时间** | **2024-12-31**
**· 🔋 上次活动** | 2024-01-15 10:30:00
**· 📅 过去30天** | 1200 分钟
```

#### 没有Emby账户的用户
```
**· 🍉 TG&名称** | [李四](tg://user?id=987654321)
**· 🍒 识别のID** | `987654321`
**· 🍓 当前状态** | 未注册
**· 🍥 持有樱花币** | 50
**· 💠 账号名称** | 无账户信息
**· 🚨 到期时间** | **无账户信息**
```

#### 白名单用户
```
**· 🍉 TG&名称** | [王五](tg://user?id=555666777)
**· 🍒 识别のID** | `555666777`
**· 🍓 当前状态** | 白名单
**· 🍥 持有樱花币** | 999
**· 💠 账号名称** | wangwu_vip
**· 🚨 到期时间** | **+ ∞**
**· 🔋 上次活动** | 2024-01-16 15:45:00
**· 📅 过去30天** | 3600 分钟
```

#### 被禁用的用户
```
**· 🍉 TG&名称** | [赵六](tg://user?id=111222333)
**· 🍒 识别のID** | `111222333`
**· 🍓 当前状态** | **已禁用**
**· 🍥 持有樱花币** | 0
**· 💠 账号名称** | zhaoliu_banned
**· 🚨 到期时间** | **2024-01-10**
**· 📅 过去30天未有记录**
```

### 2. 新用户首次查询
```
**· 🆔 TG** ：[新用户](tg://user?id=999888777) [`999888777`]
数据库中没有此ID。ta 还没有私聊过我
```

### 3. 不同配置下的到期时间显示

#### 启用活跃度检查时
```
**· 🚨 到期时间** | __若7天无观看将封禁__
```

#### 无需保号时
```
**· 🚨 到期时间** | __无需保号，放心食用__
```

## 管理员查看时的操作按钮

### 普通用户（有账户）
```
[🌟 解除禁用] [⚠️ 删除账户]
[✔️ 额外媒体库] [🚫 踢出并封禁]
[❌ 删除消息]
```

### 被禁用用户
```
[💢 禁用账户] [⚠️ 删除账户]
[✔️ 额外媒体库] [🚫 踢出并封禁]
[❌ 删除消息]
```

### 无账户用户
```
[✨ 赠送资格] [🚫 踢出并封禁]
[❌ 删除消息]
```

## 使用场景示例

### 场景1：用户自查信息
```
用户: /myinfo
Bot: [显示个人信息，60秒后自动删除]
```

### 场景2：管理员检查用户状态
```
管理员: /kk @username
Bot: [显示用户详细信息，包含管理按钮]
```

### 场景3：新用户首次使用
```
新用户: /myinfo
Bot: 数据库中没有此ID。ta 还没有私聊过我

新用户: /start (私聊bot)
Bot: 初次使用，录入数据库完成。请点击 /start 重新召唤面板

新用户: /myinfo (再次查询)
Bot: [显示基础信息，状态为"未注册"]
```

## 特殊情况处理

### 1. 频道消息
如果消息来自频道而非个人用户，命令会被忽略，不做任何处理。

### 2. 权限不足
如果用户不在指定群组中，命令不会被处理。

### 3. Emby服务器离线
如果Emby服务器无法连接，会跳过播放统计信息，只显示基础数据库信息。

### 4. 数据库连接异常
如果数据库连接失败，会显示错误信息或返回空结果。

## 隐私保护

1. **自动删除**: 所有信息显示60秒后自动删除
2. **权限控制**: 只有群组成员可以使用命令
3. **敏感信息处理**: 密码等敏感信息不会在前端显示
4. **操作日志**: 管理员操作会记录日志

## 技术细节

### 支持的命令前缀
- `/` (斜杠)
- `!` (感叹号)
- `.` (点号)
- `，` (中文逗号)
- `。` (中文句号)

### 响应时间
- 正常情况: 1-3秒
- Emby服务器慢响应: 3-10秒
- 数据库繁忙: 2-5秒

### 并发处理
系统支持多用户同时查询，每个用户的查询都是独立处理的。