# Bot隐私模式设置说明

## 问题描述

如果你发现M尊享用户欢迎词功能无法工作，或者bot无法检测到群组中用户的普通发言，这是因为bot处于**隐私模式（Privacy Mode）**。

## 什么是隐私模式？

Telegram bot默认开启隐私模式，在此模式下bot只能看到：
- 以 `/` 开头的命令
- 回复给bot的消息
- 服务消息（如成员加入/退出）
- Bot作为管理员的频道中的消息

**隐私模式下，bot无法看到群组中的普通用户消息！**

## 如何关闭隐私模式

### 步骤：

1. 在Telegram中打开 [@BotFather](https://t.me/BotFather)

2. 发送命令 `/mybots`

3. 选择你的bot

4. 点击 **Bot Settings**

5. 点击 **Group Privacy**

6. 点击 **Turn off** （关闭隐私模式）

7. 看到提示 `Privacy mode is disabled for YourBot.` 即成功

### 命令方式（快速）：

你也可以直接向 BotFather 发送：
```
/setprivacy
```
然后按照提示选择你的bot，并选择 **Disable**。

## 安全提示

⚠️ **重要**：关闭隐私模式后，bot可以看到群组中的所有消息。请确保：

1. ✅ 只在**授权群组**中添加bot（已在代码中通过 `filters.chat(group)` 限制）
2. ✅ 代码只处理必要的消息
3. ✅ 不要将bot添加到未授权的群组

## 功能说明

关闭隐私模式后，以下功能将正常工作：

- ✨ **M尊享用户欢迎词**：当M等级用户在群组中发言时，bot会自动发送欢迎消息（每次发言都会响应）
- 🎯 **反频道消息检测**：自动检测并处理频道消息（如果启用）

## 验证是否生效

关闭隐私模式后：

1. 让一个M等级用户在授权群组中发送消息
2. Bot应该会自动回复欢迎消息
3. 查看bot日志，应该能看到 `【M尊享欢迎】` 相关的日志

## 故障排查

如果关闭隐私模式后功能仍不工作：

1. **检查bot是否在授权群组中**
   - 确认群组ID在 `config.json` 的 `group` 列表中

2. **检查用户等级**
   - 确认用户在数据库中的 `lv` 字段为 `'m'`

3. **查看日志**
   - 检查bot日志中是否有错误信息

## 技术细节

相关代码文件：
- `bot/modules/extra/m_welcome.py` - M尊享用户欢迎功能
- 过滤器：`filters.chat(group) & filters.group` - 限制仅在授权群组中工作

---

**最后更新**：2024-01-09
